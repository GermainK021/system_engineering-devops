#!/usr/bin/env bash
# This script lists non-hidden files in the current directory and displays only the part after the first dash using a for loop

for file in *; do
  # Skip if it's not a regular file
  if [ -f "$file" ]; then
    echo "${file#*-}"
  fi
done
